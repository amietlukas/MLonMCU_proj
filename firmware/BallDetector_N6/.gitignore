# CubeMX-managed / auto-generated — regenerated by the CubeMX project and the
# build, never hand-edited. We track only Core/ source, scripts/, tests/,
# model/, README.md, and the .ioc (so the project is regenerable). Everything
# below is reproduced by CubeMX + stedgeai_compile.sh + build.sh.

# CubeMX-generated peripheral drivers / middleware
Drivers/
Middlewares/

# X-CUBE-AI generated network code + weights (stedgeai_compile.sh)
X-CUBE-AI/
st_ai_ws/

# CubeIDE build outputs
Debug/
Release/
build/
*.elf
*.bin
*.map
*.list

# CubeIDE workspace / launch metadata
.settings/
*.launch

# host-side test artifacts
tests/__pycache__/
scripts/__pycache__/
